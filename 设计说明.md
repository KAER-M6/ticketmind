# 工单智脑 TicketMind · 设计说明

本文说明三件事：**为什么这么搭**（设计思路与取舍）、**系统怎么运转**（架构与数据流）、**用户能用它做什么**（功能说明）。

---

## 一、要解决的问题

企业 IT 服务台的日常是：用户提交工单 → 人工判断"这属于哪一类、多紧急" → 翻历史相似案例 → 写回复。前两步高度依赖个人经验，新人容易判错；第三步是重复劳动。

系统要自动化这条链路的前 80%，但**保留人工最后一道审核**——因为工单分类出错会直接影响 SLA 与责任人，这是不能全自动的环节。

三个明确的设计目标：

| 目标 | 具体化 |
|---|---|
| 判定要准 | 工单类型分类准确率 ≥ 90%（人工基线约 74%） |
| 决策要可解释 | 每条判定给出理由、置信度、引用的历史案例，人工能快速判断对错 |
| 成本要可控 | 不引入额外的向量库/框架服务；单条工单 token 消耗不随优化膨胀 |

---

## 二、搭建思路

### 2.1 整体架构

```
                    ┌─────────────────────────────┐
   工单输入 ────────▶│  Web 审核台（单页 HTML/JS）  │
   (单条/批量导入)   └──────────────┬──────────────┘
                                    │ HTTP
                    ┌───────────────▼───────────────┐
                    │      FastAPI 服务 (app/main.py)│
                    │  鉴权中间件 · CORS · 路由      │
                    └───────────────┬───────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
┌───────▼────────┐        ┌─────────▼──────────┐      ┌─────────▼────────┐
│ Agent 1 分诊    │        │ Agent 2 检索        │      │ Agent 3 回复      │
│ triage.py      │───────▶│ retriever.py       │─────▶│ reply.py         │
│ 类型 + 优先级    │  few-shot│ 知识库相似工单      │  案例  │ 候选回复草稿      │
└───────┬────────┘  反哺   └─────────┬──────────┘      └─────────┬────────┘
        │                           │                           │
        │        ┌──────────────────▼──────────┐                │
        │        │ 语言桥接层 lang.py           │                │
        │        │ 中文工单 → 英文检索查询       │                │
        │        └─────────────────────────────┘                │
        │                                                       │
        │   ┌───────────────────┐                               │
        └──▶│ 人工反馈池         │◀──────────────────────────────┘
            │ feedback.py       │
            │ 审核纠正标签回流    │
            └─────────┬─────────┘
                      │
        ┌─────────────▼─────────────┐
        │ SQLite 状态机 (db.py)      │
        │ open→drafted→approved→resolved │
        └───────────────────────────┘
```

### 2.2 一次工单的完整旅程

以「共享网盘本周第三次中断，整个销售团队无法工作」为例：

```
① 提交        POST /api/process  →  生成工单号 TK-00xx，落库为 open
                │
② 语言判定     lang.has_cjk() 检出中文 → 1 次 LLM 调用转写为英文检索查询
                │                （英文工单跳过此步，零开销；结果带缓存）
                │
③ 混合检索     retriever 在 19,782 条英文历史工单中召回 top-5
                │  BM25 词法打分 ┐
                │  向量语义打分  ├─ RRF 融合排序 → 5 条相似案例
                │               ┘
                │
④ 动态 few-shot 把 5 条案例的真实标签（type/priority）构造成 few-shot 示例
                │  人工反馈池中若有更相似的已确认样本，占前 2 位
                │
⑤ 分诊         triage：1 次 LLM 调用 → type=Problem / priority=high
                │  reason + confidence + decision_source 一并返回
                │
⑥ 回复草稿     reply：基于 5 条案例生成候选回复，标注引用来源
                │  语言跟随工单 → 输出中文草稿
                │
⑦ 落库         status: open → drafted，等待人工审核
                │
⑧ 人工审核     通过 / 纠正标签 / 驳回
                │
⑨ 反馈回流     纠正的标签写入 feedback.jsonl → 参与后续分诊的 few-shot
                │
⑩ 完成         status → resolved
```

**关键设计**：第 ④ 步是整套系统的技术核心——**同一张工单先用检索找到"类似的已处理工单"，把它们的正确答案直接放进提示词**。LLM 需要的不是更多算力，而是更贴近当前场景的参照物。

### 2.3 技术选型与取舍

| 决策点 | 选择 | 理由 | 否决了什么 |
|---|---|---|---|
| 应用框架 | FastAPI + 自建流水线 | 三个 Agent 逻辑清晰、状态可控，出问题能直接定位到具体函数 | **不用 LangChain/LlamaIndex**：抽象层会掩盖实际调用链路，调试成本高于收益 |
| LLM | DeepSeek `deepseek-chat` | 中文理解好、价格低、OpenAI 兼容协议便于切换 | 不用本地大模型：部署成本与推理延迟都不合适 |
| 检索 | BM25 + MiniLM 向量，RRF 融合 | 词法路补齐专有名词与错误码，语义路补齐同义改写，两者互补 | 不用单一向量检索：实测混合召回 hit@5 达 99.5% |
| 分类提升 | **动态 few-shot** | token 成本不变，准确率 74.2% → 90.8% | 否决 self-consistency（多次采样投票，实测 67.5% 反低于基线，且成本 ×5，见 §四） |
| 持续进化 | **人工反馈闭环** | 零训练成本，同批样本实测 +4.0pp | 不用微调：数据量不足、迭代周期长、无法热更新 |
| 存储 | SQLite + WAL | 单机零运维，状态机语义清晰 | 不用 PostgreSQL：当前规模无必要；`db.py` 已收口连接层，未来可替换 |
| 中文支持 | **语言桥接层** | 保住原有英文索引与 BM25 通路，改动隔离在一个模块内 | 不换多语言向量模型：需重建 19,782 条索引，且 BM25 那一路仍是英文分词 |
| 交付形态 | zip + `requirements.txt` + 部署说明 | 本质是 Web 服务，依赖树含 torch | 不用 PyInstaller：打包体积会膨胀到 2GB+，且跨环境脆弱 |
| 前端 | 原生 HTML/JS 单页 | 无构建步骤，`clone` 后打开即可用，改样式无需重新打包 | 不用 Vue/React：引入工具链对当前功能无收益 |

### 2.4 各模块职责

| 模块 | 文件 | 职责 | 关键实现 |
|---|---|---|---|
| 分诊 Agent | `app/agents/triage.py` | 判定工单类型与优先级 | 动态 few-shot 构造、输出容错归一化、决策来源标记 |
| 检索 Agent | `app/agents/retriever.py` | 从知识库召回相似历史工单 | 双路召回 + RRF 融合、进程级单例、请求级缓存 |
| 回复 Agent | `app/agents/reply.py` | 生成候选回复草稿 | 基于检索案例生成，附引用来源，语言跟随工单 |
| 语言桥接 | `app/agents/lang.py` | 中文工单 → 英文检索查询；决定回复语言 | 线程安全缓存（上限 512）、失败回落原文、统计计数 |
| 反馈池 | `app/agents/feedback.py` | 存人工确认样本，供 few-shot 优先使用 | append-only JSONL、自写 TF-IDF 检索、mtime 自动重载 |
| 状态机 | `app/db.py` | 工单持久化与状态流转 | WAL 模式、单入口连接、自动迁移补列 |
| HTTP 层 | `app/main.py` | 路由、鉴权、CORS、批量并发 | `X-API-Key` 中间件、线程池并发导入 |
| 审核台 | `app/web/static/index.html` | 人工操作界面 | 单页、KPI 看板、审核时可纠正标签 |

**为什么反馈池自写 TF-IDF 而不用现成的 rank_bm25**：`rank_bm25` 的 IDF 在小样本下会退化为 0（`log(N/df)` 在 df 接近 N 时趋零），导致打分全相等、检索失效。反馈池只有千级样本，因此改用恒正形式 `log(1 + N/df)`。

### 2.5 分层原则

三条贯穿全项目的原则：

1. **失败必须可见且可降级**——检索器不可用回退静态 few-shot；LLM 输出无法解析时返回保守判定（`Request/medium`）并打 `needs_human` 标记；转写失败回落原文。任何一环挂掉，系统都给一个"能用的较差答案"，而不是报错。
2. **AI 不给绝对答案**——`priority` 字段在界面上明确标为"建议值"，每条判定都带 `reason` 与 `confidence`，让审核人 3 秒内能判断是否同意。
3. **优化必须可量化**——每次改动前后都跑评测（见 `eval/`），`golden_eval.py` 做固定集回归门禁，防止"优化 A 打坏 B"。

---

## 三、功能说明

### 3.1 功能清单

| # | 功能 | 说明 |
|---|---|---|
| 1 | 单条工单智能处理 | 粘贴工单 → 数秒内输出类型、优先级、判定理由、置信度、相似案例、候选回复 |
| 2 | 批量导入 | 粘贴多条工单（一行一条）或上传文本，可选择是否走 AI 全链路；AI 模式线程池并发（默认 6） |
| 3 | 混合检索相似案例 | 从 19,782 条历史工单中召回 top-5，双路召回 + RRF 融合，hit@5 = 99.5% |
| 4 | 回复草稿生成 | 基于检索案例生成候选回复，标注引用来源，平均引用 4 条 |
| 5 | 人工审核状态机 | 通过 / 驳回 / 完成三态流转，**通过时可下拉纠正 type 与 priority** |
| 6 | 反馈闭环 | 纠正标签自动回流语料池，参与后续分诊的 few-shot，界面显示线上准确率 |
| 7 | 中文工单支持 | 中文工单走语言桥接层检索，回复默认输出中文（`TICKETMIND_REPLY_LANG` 可强制 zh/en） |
| 8 | KPI 看板 | 工单总量、类型分布、自动化覆盖率、线上准确率、反馈池规模、检索缓存命中率 |
| 9 | 队列看板 | 按状态过滤工单列表，展开查看判定详情与草稿 |
| 10 | REST API | 10 个端点，可脱离界面对接外部系统 |
| 11 | 密钥保护 | `X-API-Key` 鉴权中间件（可选启用）、CORS 白名单 |
| 12 | 批量并发处理 | 检索结果缓存使同工单二次检索降至 0ms |

### 3.2 界面操作路径

```
打开 http://127.0.0.1:8000
  │
  ├─ 顶部 KPI          工单总量 / 待审核 / 自动化覆盖率 / 线上准确率 / 反馈语料池
  │
  ├─ 新建工单区
  │    ├─ 「填入示例工单」 一键载入中文示例
  │    ├─ 主题 + 内容 输入
  │    └─ 「AI 处理」勾选  → 提交
  │
  ├─ 处理结果区
  │    ├─ 类型徽章 + 优先级徽章 + 置信度
  │    ├─ 判定理由（reason）
  │    ├─ 相似历史案例（5 条，可展开原文）
  │    └─ 回复草稿 + 引用来源
  │
  └─ 队列看板
       ├─ 按状态筛选（全部 / 待审核 / 已通过 / 已驳回 / 已完成）
       ├─ 「通过」→ 可先纠正类型与优先级下拉，再确认（纠正值回流语料池）
       ├─ 「驳回」/「完成」
       └─ 点击行展开判定详情
```

### 3.3 API 一览

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/triage` | 仅分诊 |
| POST | `/api/process` | 端到端：分诊 → 检索 → 草稿 → 入库 |
| POST | `/api/import` | 批量导入，`process=true` 走 AI 全链路并并行 |
| GET | `/api/tickets` | 队列查询（可按 status 过滤） |
| POST | `/api/tickets/{id}/approve` | 通过审核，body 可带 `{type, priority}` 纠正标签 |
| POST | `/api/tickets/{id}/reject` | 驳回 |
| POST | `/api/tickets/{id}/resolve` | 完成 |
| GET | `/api/stats` | KPI 统计 |
| GET | `/api/feedback/stats` | 反馈池规模与人工复核口径 |
| GET | `/api/health` | 健康检查 + 检索缓存命中率 + 反馈池规模 |

### 3.4 KPI 口径定义

| 指标 | 计算方式 | 当前值 |
|---|---|---|
| 分诊 type 准确率 | 离线评估 n=500，macro-F1 0.909 | **90.8%**（基线 74.2%） |
| 优先级判定命中 | 离线评估，同批 n=500 | **78.2%**（基线 38.6%） |
| 混合检索 hit@5 | 用历史工单构造查询，看真实工单是否落在 top-5 | **99.5%** |
| 反馈闭环净收益 | 同批 100 条样本 A/B 对照，两组仅反馈池不同 | **+4.0pp**（87.0% → 91.0%） |
| 自动化覆盖率 | 已通过（含完成）工单数 / 总工单数 | 运行时计算 |
| 线上准确率 | 人工确认时未纠正的工单 / 已确认工单 | 运行时计算 |
| Golden 回归集 | 固定测试集，改动后必跑，低于阈值退出码 1 | 英文 20/20、中文 16/16 |

> 优先级指标低于 type 是预期内的：源数据集本身只有 3 级粒度标注，且边界模糊（相邻等级常可两说）。因此界面上标为"建议值"，定位为**辅助人工排序**而非最终定级。

---

## 四、关键设计决策（含被证伪的方案）

### 4.1 动态 few-shot：先做对照实验，而不是照搬教科书

提升 LLM 分类准确率的常见做法是 self-consistency（多次采样投票）。**实测下来它是负收益**：

| 方案 | 准确率 | 说明 |
|---|---|---|
| 静态 few-shot（基线） | 74.2% | 提示词里放固定示例 |
| Self-consistency（k=5, temperature=0.7） | **67.5%** | 低于基线，且成本 ×5 |
| **动态 few-shot** | **90.8%** | 检索相似工单构造示例，成本与基线相同 |

结论：对工单分类这类**依赖历史同类案例**的任务，把预算花在"给对参照物"上，比花在"多次投票"上有效得多。

### 4.2 反馈闭环而不是微调

微调需要千级以上标注数据、有训练周期、无法热更新；而工单分类的正确答案在人工审核时**天然就有**。因此把人工确认的标签存成 append-only 的 `feedback.jsonl`，分诊时用轻量 TF-IDF 在池内取最相似的 2 条，占 few-shot 前 2 位。

- **不重建向量索引**：池子规模千级，现算打分毫秒级；样本变更靠 mtime 自动重载
- **只取"已人工确认"样本**：避免模型自己的错误预测被当成正确答案回流放大
- **实测**：同批 100 条，空池 87.0% → 池内 60 条人工样本 91.0%，净收益 **+4.0pp**

### 4.3 中文支持：桥接而非替换

知识库是 19,782 条**英文**历史工单，而 BM25 的分词正则只匹配 `[a-z0-9]`，MiniLM 也是英文模型。实测中文工单直查的召回结果——**5 条全不相关，但系统照常输出判定，不报任何错**。这是典型的静默失败。

两条路可选：

| 方案 | 代价 |
|---|---|
| 换多语言向量模型 | 需重建 19,782 条索引；BM25 那一路仍是英文分词，需同步改造；评估基线全部失效 |
| **加语言桥接层** | 中文工单多 1 次 LLM 调用（约 200 token）；英文工单零开销 |

选了后者：检测到 CJK 字符时先把工单转写成英文检索查询再进原流程，改动隔离在 `lang.py` 一个模块内，原有索引与评估口径完全不动。回复语言默认跟随工单，因此英文评估集指标仍可比。

### 4.4 不用框架

三个 Agent + 一个状态机，逻辑链是线性的、显式的。用 LangChain 这类框架会把调用链路埋进抽象层，出问题时定位成本远高于自己写。项目最终只有 5 个第一方依赖（FastAPI/uvicorn/sentence-transformers/rank-bm25/python-dotenv），任何一环都能直接读源码。

---

## 五、迭代过程

| 阶段 | 做了什么 | 结果 |
|---|---|---|
| v0.1 | 数据管线 + BM25 检索 + 基础分诊 | 分诊 74.2%，链路跑通 |
| v0.2 | 动态 few-shot（+16.6pp）、优先队列、批量导入 | 分诊 **90.8%**，优先级 78.2% |
| v0.2 | 工程化：单测 54 项、ruff、golden 回归门禁、Docker、CI | 质量可控、可回归 |
| v0.2 | 人工反馈闭环 + 审核台纠正标签 | 净收益 **+4.0pp**，线上可自我进化 |
| v0.2.1 | 中文工单支持（语言桥接层）+ 中文回归集 16 条 | 中文 16/16，英文 20/20 无回归 |

迭代中被证伪并放弃的方案：self-consistency（§4.1）、多语言向量模型替换（§4.3）、PyInstaller 打包（体积 2GB+ 且脆弱）。

---

## 六、已知限制与后续规划

**限制（如实说明）**

- `priority` 源标注仅 3 级粒度，指标天然低于 type，定位为辅助建议而非定级依据
- SQLite 不支持高并发写；多用户场景需换 PostgreSQL（`db.py` 已收口连接层，替换成本低）
- 回复质量指标（token-F1 0.445 / ROUGE-L 0.297）为**转述口径**，与"直接复制历史回复"不可直接比较
- 中文经桥接翻译检索，比英文原生长链路多 1 次 LLM 调用；转写质量受模型瞬时表现影响，失败时回落原文检索
- 数据资产（parquet / 向量索引）不入版本库，首次运行需按 `README` 的「数据准备」章节重建

**规划**

- 分诊准确率向 95% 推进（扩大检索 k 值、校准标签体系）
- 反馈池规模化后评估「小模型微调」替代纯提示词方案
- 接入企业工单系统 API（Jira Service Management / 钉钉 / 企微），把审核台嵌进既有工作流
- 多语言知识库（当前仅英文历史工单，其他语种经桥接处理）

---

## 附：相关文档

| 文档 | 内容 |
|---|---|
| `README.md` | 快速开始、核心指标、数据准备 |
| `设计说明.md` | 本文：设计思路、架构、功能说明 |
| `部署说明.md` | 部署步骤、数据资产表、环境要求 |
| `eval/` | 各类评估报告原始 JSON |
