# 换行符规范化：仓库内统一 LF，避免跨平台提交产生 diff 噪音
* text=auto
*.bat text eol=crlf
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.vbs text eol=crlf

# 二进制文件：不做换行转换，diff 按 binary 处理
*.png binary
*.jpg binary
*.ico binary
*.npy binary
*.parquet binary
*.db binary
*.zip binary
