# 换行符规范化：仓库内统一 LF，避免跨平台提交产生 diff 噪音
* text=auto

# Windows 脚本：检出时必须保持 CRLF（纯 LF 会让 cmd 的 goto / 标签失效）
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 必须保持 LF 的配置/构建文件：
# Dockerfile 出现 CRLF 时，多行 RUN 的 "\\" 续行可能解析失败；
# .dockerignore / .gitignore 带 CRLF 会让模式带尾随 \r 而匹配不到。
Dockerfile text eol=lf
.dockerignore text eol=lf
.gitignore text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.toml text eol=lf
*.sh text eol=lf

# 二进制文件：不做换行转换，diff 按 binary 处理
*.png binary
*.jpg binary
*.ico binary
*.npy binary
*.parquet binary
*.db binary
*.zip binary

# scripts/create_shortcut.vbs 以 UTF-16LE + BOM 保存（脚本宿主按 ANSI 解析，
# UTF-8 存中文会乱码）。若按文本处理，git 的 CRLF 转换会在双字节字符间插入
# 孤立 0x0D 而破坏编码，因此显式声明为二进制。
*.vbs binary
