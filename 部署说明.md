# 工单智脑 TicketMind · 部署说明

## 环境要求

- Python 3.11+
- 网络可访问 DeepSeek API（`api.deepseek.com`）
- 约 500MB 磁盘空间（含依赖）

## 部署步骤

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置 API Key（首次）
copy .env.example .env
# 编辑 .env，填入 DEEPSEEK_API_KEY=sk-xxxx

# 3. 准备数据资产（从源码 / GitHub 部署时必需；用可部署 zip 包可跳过，包内已含数据）
python scripts/download_data.py   # 下载数据集 → data/raw/tickets.parquet
python scripts/preprocess.py      # en 子集清洗与划分 → data/processed/*.parquet
python scripts/build_index.py     # 构建 BM25 + 向量索引 → data/indexes/（约 1–3 分钟）

# 4. 启动
start.bat          # Windows 双击即可；桌面快捷方式见下节
# 或手动：
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

浏览器打开 <http://127.0.0.1:8000> 即可使用。

## 一键启动与停止（Windows）

日常使用不必记命令，三个入口：

| 文件 / 快捷方式 | 作用 |
|---|---|
| `start.bat` | 启动服务，就绪后自动打开浏览器 |
| `stop.bat` | 停止服务，释放 8000 端口 |
| 桌面「工单智脑」 | 指向 `start.bat` 的快捷方式（带项目图标） |
| 桌面「工单智脑-停止服务」 | 指向 `stop.bat` 的快捷方式 |

**创建桌面快捷方式**（二选一）：

```bash
python scripts/make_shortcuts.py     # 主方案：需要 pip install pywin32
```

或双击 `scripts/create_shortcut.vbs`（备选方案，无需 Python；部分安全策略会拦截脚本宿主，此时请用主方案）。

> **编辑 `.vbs` 时注意编码**：该文件以 **UTF-16LE + BOM** 保存。Windows 脚本宿主默认按 ANSI/UTF-16 解析脚本文件，若用 UTF-8 覆盖保存，中文提示会变成乱码。同理 `start.bat` / `stop.bat` 使用 **UTF-8 无 BOM + CRLF** 换行（纯 LF 会导致 `goto` 与标签失效），文件内已用 `chcp 65001` 指定代码页。

**`start.bat` 做了哪些自检**（避免双击后无声失败）：

1. **端口自检** —— 8000 已在运行则不重复启动，只打开浏览器
2. **环境自检** —— 依次探测项目内 `.venv`、托管 venv 的 Python 解释器，并校验 `fastapi`/`uvicorn` 是否已安装；缺失时给出明确提示与安装命令
3. **启动服务** —— 在独立最小化窗口（任务栏显示「TicketMind Server」）中拉起 uvicorn
4. **轮询就绪** —— 每秒探测一次 `/api/health`（免鉴权路径，返回即代表检索索引已加载），最多等 60 秒，而不是固定 sleep
5. **打开浏览器** —— 就绪后才打开，避免出现空白页
6. **失败可见** —— 任何一步失败都会保留窗口并提示原因

**停止服务**：关闭任务栏中「TicketMind Server」窗口，或双击 `stop.bat`（按端口定位进程并终止，随后验证端口确实释放）。

> 若双击后窗口一闪而过且浏览器未打开，说明自检未通过 —— 从命令行运行 `start.bat` 即可看到完整报错信息。

## Docker 部署（推荐给内网/服务器）

```bash
cp .env.example .env      # 填 DEEPSEEK_API_KEY
docker compose up -d

# 查看状态 / 日志
docker compose ps
docker compose logs -f ticketmind
```

- `data/` 与 HuggingFace 模型缓存已挂载为卷：容器重建不丢业务库、反馈池与模型
- **前提：宿主机上必须先有数据资产**。`./data` 是 bind mount，会覆盖镜像内自带的 `data/`。
  用可部署 zip 包解压即可（包内已含向量索引），或从源码先跑 `download_data.py → preprocess.py → build_index.py`。
  否则容器能起来但检索返回 503（索引缺失）。
- **torch 必须是 CPU 版**：`Dockerfile` 用 `--index-url https://download.pytorch.org/whl/cpu` 单独安装。
  Linux 上直接 `pip install torch` 会解析到 CUDA 版（2GB+，并拖入 nvidia-* 系列依赖），本项目只用 CPU 推理。
  该源不可达时覆盖：`docker build --build-arg TORCH_INDEX=<可用的 torch 镜像站> .`
- 构建慢主要在 torch；国内 pip 默认走清华源，可覆盖：
  `docker build --build-arg PIP_INDEX=https://pypi.org/simple .`
- 健康检查用的是容器自带的 `python -c "urllib..."`，因为 `python:3.12-slim` **不含 curl**
- 生产内网记得在 `compose.yml` 里打开 `TICKETMIND_API_KEY`

## 数据资产说明

| 目录 | 内容 | 必需 | 获取方式 |
|---|---|---|---|
| `data/raw/` | 原始数据集（可删除，节省 24MB） | 可选 | `scripts/download_data.py` |
| `data/processed/` | 训练/评估集 parquet（分诊 few-shot 池） | ✅ | `scripts/preprocess.py` |
| `data/indexes/` | RAG 向量索引（19,782 条，30MB） | ✅ | `scripts/build_index.py` |
| `data/fewshot.json` | 静态 few-shot 示例（检索器降级用） | ✅ | 随仓库/部署包提供，可用 `scripts/build_fewshot.py` 重建 |
| `data/feedback.jsonl` | 人工审核回流的确认样本（越用越准，建议保留/备份） | 运行时生成 | 审核台操作自动写入 |

> 从 GitHub 源码部署时，前三项需自行重建（见「部署步骤」第 3 步）；可部署 zip 包内已含全部数据资产。

> 首次启动会自动下载 MiniLM 向量模型（约 90MB，缓存在用户目录，仅一次）。

## 可选环境变量

| 变量 | 默认 | 说明 |
|---|---|---|
| `TICKETMIND_IMPORT_WORKERS` | `6` | 批量 AI 处理并发数（过高易触发 DeepSeek 连接抖动） |
| `TICKETMIND_API_KEY` | 空（关闭） | 设置后 `/api/*` 需带 `X-API-Key` 请求头 |
| `TICKETMIND_CORS_ORIGINS` | `http://127.0.0.1:8000,http://localhost:8000` | 允许的跨域来源，逗号分隔 |

## 质量门禁

```bash
pip install -r requirements-dev.txt

pytest -q                                  # 54 项单测（约 3s，不联网）
python scripts/golden_eval.py              # 英文 20 条固定回归集，type 准确率 ≥85% 才通过
python scripts/golden_eval.py --set zh     # 中文 16 条回归集
python scripts/golden_eval.py --limit 5    # 快速冒烟（5 条）
ruff check .                               # 静态检查
```

改 prompt、换模型、动检索逻辑后先跑这两个，防止优化 A 打坏 B。
`golden_eval.py` 未达标时退出码为 1，可直接挂到 CI。

## 注意事项

- 本包不含 `.env`（密钥不随包分发）
- 模型缓存目录：`~/.cache/huggingface/`，删除后首次启动会重新下载
- 打包 exe 不可行的原因：依赖链含 torch（2GB+），zip + requirements 是更合理的分发形态
